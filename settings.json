{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | { read -r cmd; if [[ $cmd =~ ^(vitest|next|prettier|npm|pnpm|yarn|git|docker)\\s ]]; then echo '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"updatedInput\": {\"command\": \"rtk '\"$cmd\"'\"}}}'; else echo '{}'; fi; }"
          }
        ]
      }
    ]
  }
}
